import Diagrama from '../models/Diagrama.js';

export const DiagramaController = {};

DiagramaController.createDiagrama = (req, res) => {
    try {
            
    } catch (error) {
        
    }
}

DiagramaController.getDiagrama = (req, res) => {
    try {
            
    } catch (error) {
        
    }
}

export default DiagramaController;